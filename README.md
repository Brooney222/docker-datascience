# Custom Datascience Docker Environment

Add readme


```
git init
git add .
git commit -m "initial commit"
git remote add origin https://github.com/Brooney222/docker-datascience.git
git push -u origin master
```

